spring:
  application:
    name: gateway-service

  cloud:
    nacos:
      server-addr: 192.168.1.97:8848
      username: cloud_developer
      password: 745700Yxy@
      # 配置中心
      config:
        file-extension: yaml
        # 引入共享配置
        shared-configs:
          - data-id: shared-config.yaml
            group: DEFAULT_GROUP
            refresh: true
        namespace: cloud
      # 服务发现
      discovery:
        server-addr: 192.168.1.97:8848
        namespace: cloud